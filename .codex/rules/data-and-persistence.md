# Dados e persistência
Hive guarda usuário temporário, progresso e cache. Firebase `eureka-9675a` fornece conteúdo publicado, com fallback seed. Carregar sob demanda. Dados reais de usuário permanecem bloqueados até autenticação e revisão LGPD. Widgets nunca conhecem SDKs de persistência.
