# Nomenclatura
Código, arquivos, classes, funções, variáveis e enums em inglês. Comentários e documentação em português. Pages usam `page_*`, widgets específicos `widget_*`, componentes globais `app_*`, tokens `ui_*`, models `model_*` e services `service_*`.
